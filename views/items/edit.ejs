<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Item — Retriever Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .fade-in-up { animation: fadeInUp .5s ease-out both; }
  </style>
</head>
<body class="bg-gray-50 text-gray-800 font-sans antialiased">

<!-- Header -->
<header class="border-b border-gray-200 bg-gradient-to-r from-blue-600 via-blue-500 to-blue-400 text-white">
  <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
    <h1 class="text-xl font-semibold">Retriever Portal</h1>
    <nav class="hidden md:flex gap-6 text-sm">
      <a href="/" class="hover:underline">Home</a>
      <a href="/items" class="underline font-medium">Items</a>
      <a href="/claimants" class="hover:underline">Claimants</a>
    </nav>
  </div>
</header>

<!-- Main -->
<main class="max-w-2xl mx-auto px-6 py-12 fade-in-up">
  <div class="bg-white shadow-lg rounded-2xl p-8">
    <h2 class="text-2xl font-bold text-blue-700 mb-6">Edit Item Details</h2>

    <form method="POST" action="/items/<%= item.id %>/edit" class="space-y-5">
      <div>
        <label class="block font-semibold mb-1 text-gray-700">Item Title</label>
        <input type="text" name="title" value="<%= item.title %>" required
               class="w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300" />
      </div>

      <div>
        <label class="block font-semibold mb-1 text-gray-700">Description</label>
        <textarea name="description" rows="3"
                  class="w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300"><%= item.description %></textarea>
      </div>

      <div>
        <label class="block font-semibold mb-1 text-gray-700">Location</label>
        <input type="text" name="location" value="<%= item.location %>" required
               class="w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300" />
      </div>

      <div>
        <label class="block font-semibold mb-1 text-gray-700">Status</label>
        <select name="status" required
                class="w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300">
          <option value="Lost" <%= item.status === 'Lost' ? 'selected' : '' %>>Lost</option>
          <option value="Found" <%= item.status === 'Found' ? 'selected' : '' %>>Found</option>
        </select>
      </div>

      <div>
        <label class="block font-semibold mb-1 text-gray-700">Category</label>
        <select name="category" required
                class="w-full border border-gray-300 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300">
          <option value="Electronics" <%= item.category === 'Electronics' ? 'selected' : '' %>>Electronics</option>
          <option value="Clothes" <%= item.category === 'Clothes' ? 'selected' : '' %>>Clothes</option>
          <option value="Stationary" <%= item.category === 'Stationary' ? 'selected' : '' %>>Stationary</option>
          <option value="Accessories" <%= item.category === 'Accessories' ? 'selected' : '' %>>Accessories</option>
          <option value="Others" <%= item.category === 'Others' ? 'selected' : '' %>>Others</option>
        </select>
      </div>

      <div class="text-right">
        <button type="submit"
                class="bg-blue-600 hover:bg-blue-700 text-white font-bold px-6 py-2 rounded-lg shadow transition">
          Save Changes
        </button>
      </div>
    </form>
  </div>
</main>

</body>
</html>
